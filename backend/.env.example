# Optional backend-local template.
# Preferred: configure the repository-root .env using scripts/configure-local.ps1.

APP_ENV=development
APP_DEBUG=true
API_PREFIX=/api
AUTO_INIT_DB=false
DATABASE_URL=sqlite+pysqlite:///./database/ona_towers.db
CORS_ORIGINS=http://localhost:3010,http://127.0.0.1:3010

ADMIN_EMAIL=admin@onatowers.dev
ADMIN_PASSWORD=ona-admin-local
ADMIN_NAME=ONA Administrator
ADMIN_ROLE=Administrator
ADMIN_DEPARTMENT=Administration
ADMIN_SESSION_SECRET=ona-local-development-secret
ADMIN_SESSION_HOURS=12

# For production, keep only placeholders in committed files and set real values
# through Vercel/Supabase environment settings. Never commit secrets.
